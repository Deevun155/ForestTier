# Forest Tier

An intelligent software engineering tool that uses a Random Forest Regressor to automatically predict Rock Band 3 custom chart difficulty based on MIDI metrics.

### Usage

This model only works on PART GUITAR. To see how to use it, see `manual.pdf`.

### Building

Information on training a model can be found in `replication.pdf`.